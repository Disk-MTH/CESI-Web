{% macro content(id, errors = [], shadow = true) %}
    <div class="input-group rounded-2 {% if shadow %}shadow{% endif %}">
        <input class="form-control" type="file" id="{{ id }}" name="{{ id }}" aria-label="{{ id }}">
    </div>
    {% if errors[id] %}
        <div class="text-coldRed ms-1">
            {{ errors[id] }}
        </div>
    {% endif %}
{% endmacro %}