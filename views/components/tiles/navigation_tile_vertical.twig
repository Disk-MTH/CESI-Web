{% macro content(index) %}
    <div class="col-lg-4 col-12 d-none d-lg-block align-self-center">
        <div class="card shadow my-3 bg-deepWarmGrey align-items-center">
            <div class="card-body text-center my-4">
                <a class="h3 text-decoration-none
            {% if index == 1 %}
               text-deepSeaBlue
            {% else %}
               text-heavyBlack
            {% endif %}
            " href="{{ url_for("wishlist", {"id": user.getId()}) }}">Souhaits</a>
                <div class="bg-heavyBlack my-4" style="border:none; height: 1px"></div>

                <a class="h3 text-decoration-none
            {% if index == 2 %}
               text-deepSeaBlue
            {% else %}
               text-heavyBlack
            {% endif %}
            " href="{{ url_for("home") }}">Historique</a>
            </div>
        </div>
    </div>
{% endmacro %}