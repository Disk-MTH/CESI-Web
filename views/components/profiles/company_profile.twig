{% macro content(company_name, activity_sector, location, employee_count, intern_count, companys_site) %}
    <div class="container">
        <div class="row align-items-center text-nowrap ">
            <h2 class="mt-3">{{ company_name }}</h2>
            <h4 class="mb-3">{{ activity_sector }}</h4>
            <h6 class="text-lightGrey">{{ location }}</h6>
            <h5 class="text-deepSeaBlue">{{ employee_count }} employés</h5>
            <h5 class="text-deepSeaBlue">Un total de {{ intern_count }} stagiaires</h5>
            <div class="mb-1"></div>
            <a class="btn btn-deepSeaBlue" href="{{ company_site }}">Consulter le site web</a>
        </div>
    </div>
{% endmacro %}